package com.msmlabs.outcoding.presentation.catdetail

import com.msmlabs.outcoding.domain.model.Cat

object CatDetailTestHelper {
    val cat = Cat(
        id = "id",
        imageUrl = "imageUrl",
        breeds = listOf()
    )
}
